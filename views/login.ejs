<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>说说 - 登录</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <style>
        div[role="alert"]{
            padding:4px 10px;
            margin-top:6px;
            margin-bottom:6px;
            display: none;
        }
    </style>
</head>

<body>
    <% include nav.ejs %>

    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1>登录</h1>
            </div>
        </div>

        <hr>

         <div class="row">
             <div class="col-lg-7 form_box form-horizontal">
                <div class="form-group">
                    <label for="inputEmail3" class="col-sm-2 control-label">Email</label>
                    <div class="col-sm-10">
                        <input type="email" data-checkvalid class="form-control" id="emailTxt" placeholder="Email">
                    </div>
                </div>
                <div class="form-group">
                    <label for="inputEmail3" class="col-sm-2 control-label">密码</label>
                    <div class="col-sm-10">
                        <input type="password" data-checkvalid class="form-control" id="pwdTxt"  >
                    </div>
                </div>
                
                <div class="form-group">
                    <div class="col-sm-offset-2 col-sm-10">
                        <button type="button" id="submitBtn" class="btn btn-success">登录</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/jquery.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <script>
        $("#submitBtn").click(function(){
            $.post("/login",{
                email : $("#emailTxt").val(),
                pwd : $("#pwdTxt").val()
            },function(data){
                if(data.result == 1){
                    // alert("恭喜，成功登录！现在你将自动跳转到首页！");
                    window.location = "/";
                }else if(data.result == -1){
                    alert("用户名不存在！");
                }else if(data.result == -2){
                    alert("用户名存在，但是密码错误！");
                }
            });
        })
    </script>
</body>
</html>